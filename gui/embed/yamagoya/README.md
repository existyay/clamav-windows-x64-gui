# YAMAGoya Embedded Files

Place YAMAGoya.exe and its dependencies here to embed them into the GUI executable.

Rules should go in a `rules/` subdirectory (e.g. `rules/sigma-rules/`).

Files with `.md` and `.txt` extensions are excluded from embedding.
